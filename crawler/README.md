# crawler

This directory is a workspace for multiple crawler projects.

## Projects

- `lottery-crawler`: Thai lottery statistics crawler

## Conventions

- Put each crawler project in its own folder.
- Each project should have its own `pyproject.toml` and lock file.
- Keep source code inside that project's package directory.
